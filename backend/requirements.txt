# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# --- Data / ORM ---
SQLAlchemy==2.0.36
# For Postgres deploys also install requirements-postgres.txt (psycopg2-binary).
# SQLite (the default) needs no extra driver.
# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
# --- Auth / security ---
PyJWT==2.10.1
bcrypt==4.2.1
# --- Matching ---
rapidfuzz==3.11.0
jellyfish==1.1.0
# --- HTTP client (provider-agnostic LLM + voice) ---
httpx==0.28.1
# --- Utilities ---
python-dotenv==1.0.1
loguru==0.7.3
